package main

// main shell to attempt to get on linux
const mainshell string = "/bin/bash"

// main shell arguments
var mainshellargs []string = []string{"-i"}

// secondary shell to attempt to get on linux
const secondaryshell string = "/bin/sh"

// secondary shell arguments
var secondaryshellargs []string = []string{"-i"}
